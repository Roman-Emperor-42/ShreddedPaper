--- a/net/minecraft/world/entity/npc/wanderingtrader/WanderingTraderSpawner.java
+++ b/net/minecraft/world/entity/npc/wanderingtrader/WanderingTraderSpawner.java
@@ -1,6 +_,8 @@
 package net.minecraft.world.entity.npc.wanderingtrader;
 
 import java.util.Optional;
+
+import io.multipaper.shreddedpaper.ShreddedPaper;
 import net.minecraft.core.BlockPos;
 import net.minecraft.server.level.ServerLevel;
 import net.minecraft.tags.BiomeTags;
@@ -90,6 +_,7 @@
             return false;
         }
 
+        ShreddedPaper.runSync(player, () -> { // ShreddedPaper - run on player's thread
         BlockPos playerPos = player.blockPosition();
         int radius = 48;
         PoiManager poiManager = level.getPoiManager();
@@ -98,7 +_,7 @@
         BlockPos spawnPosition = this.findSpawnPositionNear(level, referencePos, 48);
         if (spawnPosition != null && this.hasEnoughSpace(level, spawnPosition)) {
             if (level.getBiome(spawnPosition).is(BiomeTags.WITHOUT_WANDERING_TRADER_SPAWNS)) {
-                return false;
+                return /*false*/; // ShreddedPaper - no return value
             }
 
             WanderingTrader trader = EntityType.WANDERING_TRADER.spawn(level, wanderingTrader -> wanderingTrader.setDespawnDelay(48000), spawnPosition, EntitySpawnReason.EVENT, false, false, org.bukkit.event.entity.CreatureSpawnEvent.SpawnReason.NATURAL); // CraftBukkit // Paper - set despawnTimer before spawn events called
@@ -110,11 +_,13 @@
                 // trader.setDespawnDelay(48000); // Paper - moved above, modifiable by plugins on CreatureSpawnEvent
                 trader.setWanderTarget(referencePos);
                 trader.setHomeTo(referencePos, 16);
-                return true;
+                return /*true*/; // ShreddedPaper - no return value
             }
         }
 
-        return false;
+        return /*false*/; // ShreddedPaper - no return value
+        }); // ShreddedPaper - run on player's thread
+        return true; // ShreddedPaper - assume true
     }
 
     private void tryToSpawnLlamaFor(final ServerLevel level, final WanderingTrader trader, final int radius) {

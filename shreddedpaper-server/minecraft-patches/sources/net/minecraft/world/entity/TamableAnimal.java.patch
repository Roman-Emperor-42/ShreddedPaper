--- a/net/minecraft/world/entity/TamableAnimal.java
+++ b/net/minecraft/world/entity/TamableAnimal.java
@@ -1,6 +_,7 @@
 package net.minecraft.world.entity;
 
 import java.util.Optional;
+import io.multipaper.shreddedpaper.ShreddedPaper;
 import net.minecraft.advancements.triggers.CriteriaTriggers;
 import net.minecraft.core.BlockPos;
 import net.minecraft.core.component.DataComponents;
@@ -253,7 +_,9 @@
     public void tryToTeleportToOwner() {
         LivingEntity owner = this.getOwner();
         if (owner != null) {
+            ShreddedPaper.ensureSync(this, owner, () -> { // ShreddedPaper - run on this and owner's thread
             this.teleportToAroundBlockPos(owner.blockPosition());
+            }); // ShreddedPaper - run on this and owner's thread
         }
     }
 
@@ -276,6 +_,13 @@
     }
 
     private boolean maybeTeleportTo(final int x, final int y, final int z) {
+        // ShreddedPaper start - the random offset can push the destination across a region border into a region
+        // this thread doesn't own (even though the owner's own chunk is owned); reject such candidates so we
+        // don't crash in moveEntity - another offset will be tried, or the teleport retries next tick
+        if (this.level() instanceof ServerLevel serverLevel && !ShreddedPaper.isSync(serverLevel, new net.minecraft.world.level.ChunkPos(x >> 4, z >> 4))) {
+            return false;
+        }
+        // ShreddedPaper end
         if (!this.canTeleportTo(new BlockPos(x, y, z))) {
             return false;
         }
@@ -286,6 +_,11 @@
             return false;
         }
         org.bukkit.Location to = event.getTo();
+        // ShreddedPaper start - a plugin may have moved the event's destination into an unowned region
+        if (this.level() instanceof ServerLevel serverLevel && !ShreddedPaper.isSync(serverLevel, new net.minecraft.world.level.ChunkPos(to.getBlockX() >> 4, to.getBlockZ() >> 4))) {
+            return false;
+        }
+        // ShreddedPaper end
         this.snapTo(to.getX(), to.getY(), to.getZ(), to.getYaw(), to.getPitch());
         // CraftBukkit end
         this.navigation.stop();
